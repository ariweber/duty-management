
def input_id():
    """Prompts the user to enter a soldier ID and returns it as an integer."""
    input_id = input("Enter soldier ID: ")
    return int(input_id)

def input_name():
    """Prompts the user to enter a soldier name and returns it as a string."""
    input_name = input("Enter soldier name: ")
    return input_name.strip()